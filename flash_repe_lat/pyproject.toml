[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flash-repe-lat"
version = "0.1.0"
description = "RepE-LAT honesty probe on Llama 3.3 70B via runpod-flash"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = ["runpod-flash"]
